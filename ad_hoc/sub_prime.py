def sub_prime():
    """
    Esta função retorna um caracter "S" ou "N". Se sim o banco
    será capaz de pagar seu empréstimo, caso contrário será não,
    o banco não será capaz de pagar seu empréstimo.
    :return:
    """
    dados = input().split(" ")
    b, n = int(dados[0]), int(dados[1])


sub_prime()
